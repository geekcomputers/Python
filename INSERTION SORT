def insertion(A):
    l=len(A)
    for j in range(1,l):
        key=A[j]
        i=j-1
        while i>=0 and A[i]>key:
            A[i+1]=A[i]
            i=i-1
        A[i+1]=key

a=[10,1,5,4,3,2]
insertion(a)
print(a)

